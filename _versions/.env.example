# Copy this to .env and edit as needed.
# Host port to expose the web UI on:
HOST_PORT=8099

# Absolute path to your selected project's root (the folder containing ".taskmaster" or "taskmaster").
# Example Windows:
# PROJECT_ROOT=E:/projects/transkript2
# Example Linux:
# PROJECT_ROOT=/home/user/projects/transkript2
PROJECT_ROOT=/absolute/path/to/your/project

# Optional overrides (usually not needed):
# TASKMASTER_DIR=/workspace/.taskmaster
# TASKS_FILE=/workspace/.taskmaster/tasks/tasks.json
# STATE_FILE=/workspace/.taskmaster/state.json
# CONFIG_FILE=/workspace/.taskmaster/config.json
