{
  "name": "mcp-server-taskmaster",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal MCP stdio server for TaskMasterWeb",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn src/server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
